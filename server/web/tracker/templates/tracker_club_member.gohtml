{{ template "head" addStr "Tracker - " .DisplayName }}
<div class="container">
    <div class="container-header">
        {{ template "back_button" addStr .Club.URL "/members" }}
        <h1>
            {{ template "campfire_member_header" . }}
        </h1>
    </div>

    <div class="section">
        <div class="event-stats">
            <div class="event-stat">
                <span class="event-stat-value">{{ .TotalCheckIns }}</span>
                <span class="event-stat-label">Check-Ins</span>
            </div>
            <div class="event-stat">
                <span class="event-stat-value">{{ .TotalAccepted }}</span>
                <span class="event-stat-label">Not Checked-In</span>
            </div>
            <div class="event-stat">
                <span class="event-stat-value">{{ .TotalCheckInRate }}%</span>
                <span class="event-stat-label">Check-In Rate</span>
            </div>
        </div>
    </div>

    <div class="section">
        <div class="section-header">
            <h2>Details</h2>
            <a href="{{ .ProfileURL }}" class="button" hx-boost="true">Global Profile</a>
        </div>

        <div class="event-overview">
            <div class="event-overview-item">
                <strong>Username</strong>
                <div>{{ if .Username }}{{ .Username }}{{ else }}—{{ end }}</div>
            </div>

            <div class="event-overview-item">
                <strong>Display Name</strong>
                <div>{{ .DisplayName }}</div>
            </div>

            <div class="event-overview-item">
                <strong>Member ID</strong>
                <div><span class="mono">{{ .ID }}</span></div>
            </div>

            <div class="event-overview-item">
                <strong>Club</strong>
                <div>
                    {{ if .Club.AvatarURL }}
                        <img class="icon-32" src="{{ .Club.AvatarURL }}" alt="">
                    {{ else }}
                        <img class="icon-32" src="/static/default.png" alt="">
                    {{ end }}
                    <a href="{{ .Club.URL }}" title="{{ .Club.ID }}" hx-boost="true">{{ .Club.Name }}</a>
                </div>
            </div>

            <div class="event-overview-item">
                <strong>Imported</strong>
                <div>
                    {{ if not .ImportedAt.IsZero }}
                        {{ formatTimeToRelDayTime .ImportedAt }}
                    {{ else }}
                        —
                    {{ end }}
                </div>
            </div>
        </div>
    </div>

    <div class="section">
        <div class="section-header">
            <h2>Check-Ins</h2>
        </div>
        <ul class="list">
            {{ range $event := .Events }}
                {{ template "event_list_item" $event }}
            {{ else }}
                <span>No events found.</span>
            {{ end }}
        </ul>

        <div class="section-header event-members-subheader">
            <h2>Not Checked-In</h2>
        </div>
        <ul class="list">
            {{ range $event := .AcceptedEvents }}
                {{ template "event_list_item" $event }}
            {{ else }}
                <span>No events found.</span>
            {{ end }}
        </ul>
    </div>
</div>
{{ template "tracker_footer" }}
