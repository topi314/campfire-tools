query ArchivedMeetupsMembers_Query(
    $eventId: ID!
    $first: Int!
    $after: String
) {
    event(id: $eventId) {
        id
        members(first: $first, after: $after) {
            totalCount
            edges {
                node {
                    id
                    username
                    displayName
                    avatarUrl
                    badges {
                        badgeType
                        alias
                    }
                    gameProfiles {
                        id
                        game
                        codename
                        displayName
                        level
                        faction
                        factionColor
                        visibility
                        lastPlayedTimestampMs
                    }
                }
            }
            pageInfo {
                hasNextPage
                startCursor
                endCursor
            }
        }
    }
}


